
/**
 * Collez ici vos exports d'images (format JSON exporté depuis l'app) 
 * pour un chargement instantané sans quota API.
 */
export const SEED_IMAGES: Record<string, string[]> = {
  // Format: "persona_id": ["base64_img1", "base64_img2", ...]
};
